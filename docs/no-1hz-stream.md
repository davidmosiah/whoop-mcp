# No 1 Hz stream API

WHOOP public API does **not** expose continuous 1 Hz streams.
This package will **not** invent `whoop_heart_series` at 1 Hz.
Use recovery/sleep/strain summaries and existing tools only.

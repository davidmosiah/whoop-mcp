# WHOOP recovery demo (synthetic)

Real device screen recording may be BLOCKED-EXTERNAL. Synthetic path:

1. `whoop_demo` → contract-shaped recovery/sleep/strain
2. `whoop_connection_status` before live
3. Live: `whoop_daily_summary` then recovery tools

## API limit
WHOOP official API does **not** expose continuous 1 Hz streams — do not invent agent-safe series for WHOOP.
